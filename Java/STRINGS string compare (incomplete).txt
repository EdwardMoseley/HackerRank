import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class Solution {
    
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        
        String str = sc.nextLine();
        int n = sc.nextInt();
        
        int[] count = new int[str.length()];
        String[] store = new String[str.length()];
        
        //System.out.println(str);
        //System.out.println(n);
        
        for (int i = 0; i<=str.length()-n; i++){
            store[i] = str.substring(i,i+n);
            //System.out.println(store[i].charAt(0));
            //if (store[i].charAt(0).compareTo(st)){
            
            //}
            //Note: store[i].charAt(whatever int) works
            //Note: compareTo(String anotherString) 
            //Compares two strings lexicographically.
        }
        
        
        
        
                
        System.out.println(Arrays.toString(store));
    }
}