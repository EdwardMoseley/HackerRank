declare -i x=0
read n
loop=true
while $loop
    do
        read || loop=false
        x=$(( x + REPLY ))
done
#Note: wrong answer, need to maintain remainder
printf "%.3f" $(echo "scale=3; $(( $x/$n ))" | bc -l)