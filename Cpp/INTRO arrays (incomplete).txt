#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int n;
    scanf("%d", &n);
    int arr[n];
    //printf("%d", n);
    int i = n-1;
    //int j = 0;
    while(i >= 0){
        cin >> arr[i];
        //cout << arr[j];
        i = i - 1;
	//j = j - 1;
    }
    return 0;//arr[2];
}
